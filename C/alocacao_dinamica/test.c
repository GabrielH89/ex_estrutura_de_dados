
#include <stdio.h>

int main() {
    //Variáveis ficam armazenadas na memória RAM
    int a = 10; 
    int b = 20; 
    int sum = a + b; 

    printf("Sum: %d\n", sum); // Soma feita pelo processador
    return 0;
}
