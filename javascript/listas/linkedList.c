#include <stdio.h>
#include <math.h>
#include <stdlib.h>

typedef struct No {
    int valor;
    scruct No *proximo;
}No;

typedef struct {
    No *inicio;
    int tam;
}Lista; 

void inserirInicio(Lista *lista, int valor) {
    No *novo = malloc(sizeof(No))
}

int main() {
    Lista lista;
    
    lista.inicio = NULL;
    lista.tam = 0;

    return 0;
}


