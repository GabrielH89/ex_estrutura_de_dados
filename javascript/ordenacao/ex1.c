/*#include <stdio.h>

int main() {
    int option;
    do{
        int number;
        printf("Insert a number: \n");
        scanf("%d", &number);

        if(number % 2 == 0){
            printf("O numero %d e par \n", number);
        }else{
            printf("O numero %d e impar \n", number);
        }

        printf("Deseja realizar mais uma verficacao? [1-yes/2-no] \n");
        scanf("%d", &option);
    }while(option == 1);
    
    printf("Programa finalizado \n");
    return 0;
}*/

/*#include <math.h>

int main() {
    int array[] = {5, 4, 10, 18, 7};
    int n = sizeof(array) / sizeof(array[0]);
    int menor = array[0];
    int maior = array[0];

    for(int c=0; c<=n; c++){
        if(menor > array[c]){
            menor = array[c];
        }
        if(maior < array[c]){
            maior = array[c];
        }
    }

    printf("O menor valor do array e %d \n", menor);
    printf("O maior valor do array e %d \n", maior);
}*/



