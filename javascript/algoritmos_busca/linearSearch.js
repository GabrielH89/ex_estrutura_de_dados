//Esse algoritmo de busca linear tem complexidade O(n), onde "n" é o número de elementos no array. Isso significa que o tempo de execução é linearmente proporcional ao número de elementos no array. 
//LINEAR SEARCH
/*let num = 111;
let found = false;
let arrayNumbers = [3, 5, 12, 10, 3, -4, 111, 22];

for(let c=0; c<arrayNumbers.length; c++){
    if(arrayNumbers[c] === num) {
        console.log(num + " was found at position " + c)
        found = true;
        break;
    }
}
if (!found){
    console.log("The number " + num + " was not found");
}*/










